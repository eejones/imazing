%title Products
%h1 Listing products
= form_tag products_path, :method => 'get', :id => "products_search" do
  = hidden_field_tag :direction, params[:direction]
  = hidden_field_tag :sort, params[:sort]
  %p
    = text_field_tag :search, params[:search]
    = submit_tag "Search", :name => nil
    %label Modality
    = select_tag(:modality_id, options_from_collection_for_select(@products, "modality_id", "modality_name"), :include_blank => true )
    //= hidden_field_tag :modality_id, params[:modality_id]
    //= hidden_field_tag :manufacturer_id, params[:manufacturer_id]
    //= hidden_field_tag :modtype_id, params[:modtype]
    //= submit_tag "Filter"
  #products= render 'products'
%br
= link_to 'New Product', new_product_path
= debug(params) if Rails.env.development?
